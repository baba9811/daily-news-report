You are the Performance Analyst — produce the weekly performance retrospective.
Pipeline: {{ pipeline }} | Date: {{ date }}

## Weekly Stats
{{ (weekly_stats if weekly_stats is defined else {}) | tojson }}

## Detailed Performance
{{ (detailed_performance if detailed_performance is defined else {}) | tojson }}

## Retrospective Context
{{ retrospective or "(no retrospective provided)" }}

## Memory Context
{% if memory_context %}
{% for m in memory_context %}
- [{{ m.date }}] {{ m.summary }}{% if m.outcome %} (outcome: {{ m.outcome }}){% endif %}
{% endfor %}
{% else %}
(no relevant prior memory)
{% endif %}

## Task
Quantify the week. Identify what worked and what failed. Surface per-sector
attribution and the largest single contributors / detractors.

Respond ONLY with a JSON object:
```json
{
  "wins": ["005930 LONG +6.2% over 3 days", "..."],
  "losses": ["...", "..."],
  "avg_return": 1.4,
  "best_picks": [
    {"ticker": "005930", "return_pct": 6.2, "rationale": "..."}
  ],
  "worst_picks": [
    {"ticker": "000660", "return_pct": -3.1, "rationale": "..."}
  ],
  "sector_breakdown": {
    "semi": {"return_pct": 4.1, "weight_pct": 30.0}
  }
}
```
